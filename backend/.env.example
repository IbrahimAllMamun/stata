DATABASE_URL="db_url"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"
PORT=3000
NODE_ENV=development
MAX_FILE_SIZE=2097152
UPLOAD_PATH=uploads
ALLOWED_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173
SEED_ADMIN_USERNAME="usr"
SEED_ADMIN_PASSWORD="pwd"

# The address that campaign emails are sent FROM (required)
EMAIL_FROM=no-reply@yourdomain.com
EMAIL_FROM_NAME=STATA

# SMTP server credentials – examples below cover common providers:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=no-reply@yourdomain.com
SMTP_PASS=<password for no-reply@yourdomain.com>


# Individual email account (send + receive from info@)
IMAP_HOST=yourdomain.com
IMAP_PORT=993
INFO_EMAIL=info@yourdomain.com
INFO_EMAIL_PASS=<password for info@yourdomain.com>